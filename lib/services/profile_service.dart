// Este archivo está obsoleto por ahora. 
// Usar GarageService para conectar con el backend Python.

import 'package:motoriders_app/models/motorcycle_model.dart';

class ProfileService {
  // Mantenemos métodos vacíos por si algún archivo viejo los llama
  Future<void> addMotorcycle(Motorcycle newMoto) async {
    // No hace nada
  }
}