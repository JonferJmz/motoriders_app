name: motoriders_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  crypto: ^3.0.3
  http: ^1.2.1
  google_maps_flutter: ^2.5.3
  geolocator: ^11.0.0
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1 # <-- DEPENDENCIA AÑADIDA

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/utils/map_style.json
